{
  "name": "escalas-app",
  "version": "0.1.0",
  "private": true,
  "description": "Aplicacao Next.js para o projeto escalas-app.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "npm run test:unit && npm run test:integration && npm run test:smoke",
    "test:unit": "node --test --experimental-default-type=module tests/unit/*.test.mjs",
    "test:integration": "node --test --experimental-default-type=module tests/integration/*.test.mjs tests/auth/*.test.mjs",
    "test:smoke": "node --test --experimental-default-type=module tests/smoke/*.test.mjs",
    "test:auth": "node --test --experimental-default-type=module tests/auth/*.test.mjs",
    "migrate:components:backup-prd:dry": "node scripts/migrations/migrate-components-backup-prd.cjs --dry-run",
    "migrate:components:backup-prd": "node scripts/migrations/migrate-components-backup-prd.cjs"
  },
  "engines": {
    "node": "22.14.0"
  },
  "dependencies": {
    "mongodb": "^7.1.1",
    "next": "^15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "eslint-config-next": "^15.0.0"
  }
}
